// Main application initialization
document.addEventListener('DOMContentLoaded', function() {
  // Setup UI components
  UI.setupInstructions();
  
  // Setup event handlers
  Events.setup();
  
  // Setup resizers
  Resizers.setup();
  
  //